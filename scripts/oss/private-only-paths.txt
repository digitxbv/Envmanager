# PRIVATE-ONLY paths — removed from the public subset.
# Format: paths and globs as understood by git-filter-repo --invert-paths.
# Lines starting with # are comments and are stripped by the script.

# --- Stripe edge functions (billing backend) ---
supabase/functions/stripe-checkout/
supabase/functions/stripe-portal/
supabase/functions/stripe-webhook/

# --- Billing UI (Stripe) ---
# NOTE: the core limit/lock UI (LimitReachedModal, LockedOverlay) was moved to
# app/components/limits/ (kept) because the dashboard layout imports it. Only
# Stripe-specific billing UI (UsageCard.vue) remains under app/components/billing/.
app/components/billing/
app/pages/dashboard/billing.vue
app/pages/dashboard/settings/billing.vue

# --- Marketing pages ---
app/pages/index.vue
app/pages/pricing.vue
app/pages/contact.vue
app/pages/features/
app/pages/compare/
app/pages/lp/
app/pages/solutions/
app/pages/for-agencies.vue
app/pages/for-developers.vue
app/pages/for-enterprises.vue
app/pages/for-startups.vue
app/pages/integrations/
app/pages/email/

# --- Legal pages ---
app/pages/cookie-policy.vue
app/pages/disclaimer.vue
app/pages/privacy.vue
app/pages/terms.vue

# --- Blog + content ---
app/pages/blog/
app/pages/docs/
content/blog/
content/docs/
content/marketing/
content/legal/

# --- Docs components (render content/docs — private) ---
app/components/docs/

# --- Content components (used only by excluded blog/docs/marketing pages) ---
app/components/content/

# --- Marketing components & layouts ---
app/components/marketing/
app/layouts/landing.vue
app/layouts/blog.vue
app/layouts/docs.vue

# --- Hosted-SaaS-only edge functions ---
# scheduled-lifecycle-emails: SaaS marketing drip scheduler.
# report-proxy-usage: reports proxy overage to Stripe (metered billing).
# contact-form: backs the excluded marketing /contact page.
# unsubscribe: backs the excluded app/pages/email/unsubscribe.vue page.
# (send-lifecycle-email is KEPT: welcome-only, invoked by core onboarding, inert without SMTP.)
supabase/functions/scheduled-lifecycle-emails/
supabase/functions/report-proxy-usage/
supabase/functions/contact-form/
supabase/functions/unsubscribe/

# --- Internal-only assets, scripts, docs ---
# NOTE: docs/ is NOT excluded wholesale — docs/self-hosting/ and docs/assets/
# are the public operator docs the README links to. Only internal docs below.
.gads.json
scripts/create-posthog-dashboards.mjs
docs/extracted-prompts/
docs/launch/
docs/prds/
docs/superpowers/
docs/incident-response.md
docs/SECURITY-RELEASE.md
docs/SYNC.md

# --- ee/ hosted-only dir (never reaches public) ---
ee/

# --- Internal working dirs (not part of OSS product) ---
marketing/
launch-screenshots/
screenshots/
ds-bundle/
.design-sync/
.powermode/

# --- Maintainer agent/editor config (personal; not part of OSS product) ---
# .claude/ holds a private product-marketing context file plus per-machine
# settings (absolute home paths, references to unrelated private projects).
# .mcp.json is the maintainer's local MCP wiring, unrelated to EnvManager.
.claude/
.mcp.json

# --- SaaS production CI (not for the public repo) ---
# deploy.yml pushes the hosted image to Docker Hub and runs `supabase db push`
# + edge-function deploys against the PRODUCTION project (exposes every prod
# secret/var name and would self-trigger on the public repo). Public CI is
# test.yml + security.yml + release-image.yml + cli-publish.yml.
.github/workflows/deploy.yml

# --- Marketing/screenshot demo seeders (not wired into db reset) ---
# config.toml seeds only ./seed.sql; these are manual screenshot fixtures and
# contain real personal/third-party emails. seed.sql itself stays (scrubbed).
supabase/demo-seed.sql
supabase/demo-polish.sql
